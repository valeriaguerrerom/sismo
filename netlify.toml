# Despliegue del frontend en Netlify (RNF-07).
# Define VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY y VITE_API_URL en el panel de Netlify.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
