logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Raw seismic data (MiniSEED crudos, ~215 MB): NO se versionan.
# En producción se sirven desde Supabase Storage (bucket público mseed-raw),
# leídos por backend/api/waveforms.py. En local se leen de esta carpeta.
backend/data_raw/*
!backend/data_raw/README.md

# NOTA: public/data/cm (JSON procesados, ~136 MB) SÍ se versiona: el frontend
# lo necesita en el Explorador y Vercel lo sirve como estático con el build.

# Credenciales / entorno (nunca al repo)
.env
.env.*
!.env.example
backend/.env
*.pem
*.key

# Python: caché de bytecode y artefactos de test
__pycache__/
*.pyc
.pytest_cache/
